greedy: Al lanzarlo se llama al algoritmo greedy para cada conjunto de datos con 5 semillas distintas en cada iteración además de para cada conjunto de restricciones
Para lanzar el algoritmo basta con entrar en la carpte bin y lanzar ./ejecuta, ya están preparadas las semillas y el progrma está listo para ejecutar los 7 algoritmos para cada conjunto de datos y restricciones,

Para probar un conjunto de datos en concreto hay que llamar al programa de la siguiente forma ./P2 <numero cluster> <numero poblaciones> <path a los datos> <path a las restricciones> <semilla>
