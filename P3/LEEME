Para lanzar el algoritmo basta con entrar en la carpeta bin y lanzar ./ejecuta, ya están preparadas las semillas y el programa está listo para ejecutar los 4 algoritmos para cada conjunto de datos y restricciones,

Para probar un conjunto de datos en concreto hay que llamar al programa de la siguiente forma ./P3 <numero cluster> <path a los datos> <path a las restricciones> <semilla>
